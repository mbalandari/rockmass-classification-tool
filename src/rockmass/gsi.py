"""
gsi.py
------
Implements Hoek–Marinos GSI system using lookup tables for structure and surface condition.
"""


def compute_gsi(input: GSIInput) -> (float, dict):
    """
    Compute GSI value and breakdown.
    GSI = structure_base + surface_adjustment
    """
    ...
